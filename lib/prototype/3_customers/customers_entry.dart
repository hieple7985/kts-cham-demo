import 'package:flutter/material.dart';

class CustomersEntry extends StatelessWidget {
  const CustomersEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Customers (prototype placeholder)')),
    );
  }
}
