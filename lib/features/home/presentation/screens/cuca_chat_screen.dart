import 'package:flutter/material.dart';

class CucaChatScreen extends StatelessWidget {
  const CucaChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hỏi CUCA')),
      body: const Center(
        child: Text('Tính năng AI Chat đang phát triển.'),
      ),
    );
  }
}

