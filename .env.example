## Flutter (web-first) integration env examples
#
# This project uses `--dart-define` (see README) to inject values at build/run time.
# Do NOT commit real keys; copy to `.env` locally if you want, but you'll still need
# to pass values via `--dart-define` (or update build scripts) to make Flutter read them.

CUCA_USE_SUPABASE_AUTH=true

# Supabase
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Node API (Fastify)
CUCA_NODE_API_BASE_URL=http://localhost:4000

